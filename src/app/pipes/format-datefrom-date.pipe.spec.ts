import { FormatDatefromDatePipe } from './format-datefrom-date.pipe';

describe('FormatDatefromDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDatefromDatePipe();
    expect(pipe).toBeTruthy();
  });
});
