import { FilterReservaPipe } from './filter-reserva.pipe';

describe('FilterReservaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterReservaPipe();
    expect(pipe).toBeTruthy();
  });
});
